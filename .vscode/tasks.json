{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make",
            "type": "shell",
            "command": "make",
            "args": [
                "-C",
                "${workspaceFolder}/${input:exercise}",
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Build the project using the Makefile"
        }
    ],
    "inputs": [
        {
            "id": "exercise",
            "type": "promptString",
            "description": "Relative path to exercise directory",
            "default": "CPP00/ex01"
    }
  ]
}